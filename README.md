# yd-skillset

Personal [Agent Skills](https://www.agensi.io/learn/agent-skills-open-standard) packaged as one repo that installs into **both Claude Code and Codex CLI**. The skills themselves are plain `skills/<name>/SKILL.md` files (the open standard both harnesses read natively); a small generator emits each harness's manifest so there's a single source of truth.

## Skills

<!-- SKILLS:START (generated by scripts/generate.py — do not edit) -->

| Skill | What it does |
| --- | --- |
| `algo-rewrite` | Whole-codebase algorithmic rewrite pipeline for any language — inventory every algorithm, data flow, and feature with CPU/memory complexity, triage through SIMD / concurrency / library-first / hot-cold lenses, ideate greenfield replacements under a no-compatibility rule, adversarially refute ideas in independent agents, settle survivors by A/B/C benchmark shootout (red-first TDD, zero-alloc hot paths, two-axis speed-vs-simplicity adoption), assemble winners into a fresh-file sibling v2 with class-map tests, a blind adversarial edge audit, and oracle-seeded fuzzing, audit feature parity with a gap-hunter agent that can force full re-iteration, and finish with the harden skill. |
| `harden` | Iterative, low-noise hardening review that only fixes proven defects — boundary violations, concurrency bugs (data races/deadlocks), resource/lifetime errors (leaks, use-after-free, double-release, dangling references), and garbage/uninitialized data. |
| `mastermind` | Meta-skill that turns the expensive main-thread model into a pure decision-maker — every file read, search, build, edit, and web lookup is executed by cheaper-model subagents; the main thread is forbidden all direct IO and tools except agent dispatch, skill invocation, and the todo list. |
| `msvcpp-toolkit` | C++ language toolkit, MSVC/Windows-first — the language-specific half of the algo-rewrite and harden skills. |
| `test-practice` | Language-agnostic testing rulebook — the testing half of algo-rewrite and harden. |

<!-- SKILLS:END -->

## Install

### Claude Code

From GitHub:

```bash
claude plugin marketplace add yuridevx/yd-skillset
claude plugin install yd-skillset@yd-skillset
```

Local checkout (dev):

```bash
claude plugin marketplace add /s/yd-skillset
claude plugin install yd-skillset@yd-skillset --scope local
```

Or launch a session pointed straight at the checkout, no install:

```bash
claude --plugin-dir /s/yd-skillset
```

### Codex CLI

From GitHub or a local marketplace root:

```bash
codex plugin marketplace add yuridevx/yd-skillset      # GitHub shorthand
codex plugin marketplace add /s/yd-skillset            # local checkout
```

Then pick `yd-skillset` in the Codex plugin directory and install.

## Repo layout

```
plugin.source.json              # SINGLE SOURCE OF TRUTH — repo-level metadata
skills/<name>/SKILL.md          # the skills, shared verbatim by both harnesses
scripts/generate.py             # emits every manifest + the catalog above
.claude-plugin/plugin.json      # generated — Claude plugin manifest
.claude-plugin/marketplace.json # generated — Claude marketplace registry
.codex-plugin/plugin.json       # generated — Codex plugin manifest
.agents/plugins/marketplace.json# generated — Codex marketplace registry
AGENTS.md / CLAUDE.md           # generated catalog mirrors (Codex / Claude read these)
```

## Adding or changing a skill

1. Add `skills/<name>/SKILL.md` (YAML frontmatter with `name` + `description`; `name` must equal the folder name), or edit repo metadata in `plugin.source.json`.
2. Regenerate every manifest and catalog:

   ```bash
   python scripts/generate.py
   ```

3. Commit. CI / a pre-commit hook can enforce freshness with:

   ```bash
   python scripts/generate.py --check   # exits 1 if any generated file is stale
   ```

Never hand-edit the four `*.json` manifests or the `SKILLS` block in the docs — they're overwritten on the next generate.

## License

MIT — see [LICENSE](LICENSE).
